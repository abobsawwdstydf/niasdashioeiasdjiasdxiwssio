{
  "name": "nexo-mobile",
  "version": "1.0.0",
  "description": "Nexo Messenger Mobile",
  "scripts": {
    "build": "npx cap sync",
    "open:android": "npx cap open android",
    "build:android": "npx cap build android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  }
}
